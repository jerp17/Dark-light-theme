# Dark-light-theme
Toggle the classlist to change the theme
CSS variables for colors such as background, text, and accent.
Created two theme configurations using CSS custom properties.
Added a toggle switch or button to change themes.
Added JavaScript to switch theme values dynamically.
Stored user theme preference in LocalStorage.
Loaded saved theme preference on page refresh.
Refactored CSS to minimize duplication.
